<div class="row">
	<% if @vereadores.any? %>
	  <div class="col-md-12 vereadorCarousel">
	      <% @vereadores.each do |vereador| %>
	        <div class="slide">
	          <%= link_to image_tag(vereador.avatar_url(:mini)), vereador %>
	        </div>
	    <% end %>
	  </div>
  <% end %>
</div>
 
    
